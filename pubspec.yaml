name: flutter_application_1
description: A new Flutter project.

publish_to: 'none' # Prevents accidental publishing to pub.dev.

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0" # Update based on your installed Dart version.

dependencies:
  flutter:
    sdk: flutter

  # Add other dependencies as needed.

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Linter rules for Flutter apps.
  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  assets:
    - images/us.jpg
